# Сикрет создаём руками.
---
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager
  namespace: monitoring
type: Opaque
stringData:
  SMTP_PASSWORD: "smtp password"
  SMTP_USER: "smtp user"
  BOT_TOKEN: "int64 bot token"
  CHAT_ID: "chat id"

