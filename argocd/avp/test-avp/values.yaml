# Default values for test-avp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

secret:
  login: <path:test/data/user#login>
  password: <path:test/data/user#password>