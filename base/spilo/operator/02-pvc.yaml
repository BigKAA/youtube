apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test1-backup
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi