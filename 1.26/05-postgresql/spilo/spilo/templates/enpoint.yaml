---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ include "postgres-cluster.fullname" . }}
  labels:
    application: spilo
    spilo-cluster: {{ include "postgres-cluster.fullname" . }}
subsets: []
