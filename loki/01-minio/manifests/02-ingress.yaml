---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  namespace: loki
  annotations:
    kubernetes.io/ingress.class: "system-ingress"
spec:
  rules:
    - host: "minio.kryukov.local"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mi
                port:
                  name: minio
